# TODO

> **Claude에게**: 이 파일에 있는 작업만 수행하세요.
> 완료한 항목은 이 파일에서 삭제하고 CHANGELOG.md에 기록하세요.
> PROJECT_SPEC.md와 UI_GUIDELINE.md를 먼저 읽고 시작하세요.
>
> **이 문서의 역할**: "지금 무엇을 해야 하는지"만 다룹니다 — 현재 버전 확인 항목, 다음 후보 기능, 버그.
> 프로젝트 구조·스키마·완료된 기능 이력 같은 참고 정보는 여기 적지 말고 **PROJECT_SPEC.md**에 적어주세요.
> 지난 버전의 확인 항목은 배포·확인이 끝나면 이 파일에서 지웁니다 (기록은 CHANGELOG.md에 이미 남아있음).
> **v0.0.2부터**: "스프린트"라는 용어 대신 "버전"(v0.0.x)으로 통일해서 부릅니다 (Sprint 1~28은 버전 체계 도입 이전 기록이라 그대로 둠, Sprint 29 = v0.0.1).
> **v0.0.16부터**: "고민 중"(결정 안 된 아이디어)과 "다음 후보"(우선순위 미정 기능)를 하나로 합쳐 **"다음 후보"** 한 섹션에서만 관리합니다.

---

## ⚠️ 새 대화로 이어갈 때 먼저 읽어주세요 (v0.0.40 인계 노트)

지금까지의 자세한 변경 이력은 CHANGELOG.md에 버전별로 다 남아있고, PROJECT_SPEC.md·UI_GUIDELINE.md는 항상 최신 상태로 맞춰뒀으니 이 파일들을 먼저 읽으면 맥락 파악이 될 거예요.

### 🆕 v0.0.40 — 체크리스트 커스터마이징(준비물 팩·사용자 정의·탭 표시/캘린더 연동 설정)
- **탭이 "숫자 위치" 대신 "key" 기준으로 동작하도록 `js/checklist.js`를 리팩터링함** — 탭을 숨길 수 있게 되면서 기존의 `S.clTab===0/1/2/3` 같은 위치 기반 분기가 전부 깨지므로, `getVisibleTabDefs(child)[S.clTab]?.key`로 바꿈. `S.clTab`은 Firestore에 저장 안 되는 세션 전용 값이라 기존 데이터 영향 없음
- **준비물형(플랫) 체크리스트 신규**(`data/checklist-packs.js`) — 외출 준비물/100일 준비/돌 준비/돌사진 준비 4종. 월령 인덱싱 없이 카테고리 1개짜리라 기존 렌더링 파이프라인을 그대로 재사용(신규 렌더 코드 없음). 새 팩 추가는 이 파일에 객체 하나만 더하면 됨
- **사용자 정의 체크리스트** — 설정 탭 "체크리스트 관리"에서 이름+초기 항목으로 직접 만들 수 있음(`S.customChecklists`)
- **탭 표시/숨김 + 캘린더 연동 켜기/끄기** — 설정 탭에서 전부 관리(`S.clSettings`). 캘린더 연동 토글은 실제로 연동 매핑이 있는 예방접종/발달 탭에만 보임
- **⚠️ Vercel 자동 배포 외에 별도 조치 필요 없음** — Firebase 콘솔 설정이나 CLI 배포가 필요했던 v0.0.38/39와 달리, 이번 건 순수 앱 본체 변경이라 GitHub push만으로 반영됨
- 자세한 구조는 `docs/product-specs/checklist-customization.md` 참고 — 특히 "알려진 한계"에 guide 페이지 미반영·항목 편집 UI 없음 등 다음 후보가 정리돼있음

### 🔧 v0.0.39 — 관리자 전용 푸시 발송(admin.html) 신규
- 앱 본체(SPA) 밖에 완전히 분리된 `admin.html` 신규 — 제목/내용/대상(전체·임산부·연령별·특정 UID) 입력, 즉시·예약 발송, 발송 이력
- 관리자 판별은 Firestore 필드가 아니라 **Firebase Auth 커스텀 클레임**(`admin===true`) — `functions/scripts/set-admin-claim.cjs`로 로컬에서 1회성 부여
- **⚠️ 아직 코드만 완성된 상태 — 아래 "배포 필요" 섹션의 Firestore 보안 규칙 추가 + 관리자 클레임 부여가 끝나야 실제로 동작함**
- 자세한 구조·배포 체크리스트는 `docs/product-specs/admin-push.md` 참고

> v0.0.15~v0.0.24, v0.0.37, v0.0.38 인계 노트는 CHANGELOG.md에 동일 내용이 더 자세히 남아있어 정리했습니다(버전 번호로 검색하면 찾을 수 있음). v0.0.38의 "⚠️ 배포 필요" 항목은 아직 안 끝나서 아래에 그대로 남겨뒀습니다.

## ⚠️ 배포 필요 (옹짐꾼님 액션 대기 — v0.0.38 FCM 2단계)

~~코드는 다 준비됐지만, Cloud Functions는 로컬에서 CLI로 직접 배포해야 함~~ → **`dailyPushCheck` 배포 완료됨**(`Deploy complete!` 확인됨). 남은 건 실제 발송 테스트뿐:

1. ~~firebase-tools 설치, 로그인~~ (완료)
2. ~~`npm run deploy`~~ (완료)
3. ~~Firebase 콘솔 → Functions에서 `dailyPushCheck` 배포 확인~~ (완료)
4. **다음 확인 필요**: Firebase 콘솔 → Functions → `dailyPushCheck` → "테스트 실행"으로 즉시 1회 실행 — 예방접종/정부지원/오늘 일정이 있는 계정으로 로그인한 상태에서 실행해보고, 앱을 완전히 꺼둔 채로 알림이 오는지 확인 (해당하는 일정이 없으면 알림이 안 오는 게 정상)
5. 실제 스케줄(매일 09:00 Asia/Seoul)로도 며칠 지켜보면서 정상 작동하는지 확인
6. 문제가 있으면 Firebase 콘솔 → Functions → 로그(또는 `firebase functions:log`)에서 에러 확인

자세한 배경·구조는 `docs/product-specs/push-notifications.md` 참고.

## ⚠️ 배포 필요 (옹짐꾼님 액션 대기 — v0.0.39 관리자 푸시 발송)

코드는 다 준비됐지만, 아래 두 가지는 Claude가 대신할 수 없는 영역(Firebase 콘솔 전용 설정 + 로컬 Admin SDK 실행)이라 직접 해주셔야 실제로 동작함:

1. **Firestore 보안 규칙 추가** — 콘솔 → Firestore Database → 규칙에 `docs/product-specs/admin-push.md`에 있는 `adminBroadcasts` 규칙 블록을 그대로 추가 (이게 없으면 관리자 아닌 로그인 사용자도 발송을 시도할 수 있는 상태라 **반드시 먼저** 해야 함)
2. **관리자 계정 지정** — `cd functions && node scripts/set-admin-claim.cjs <옹짐꾼님 UID>` 실행 → 로그아웃 후 재로그인
3. `cd functions && npm run deploy`로 `onBroadcastCreated`/`processScheduledBroadcasts` 배포
4. https://momcal.app/admin.html 접속해서 테스트 발송 1건(즉시) + 예약 발송 1건 확인 — 예약 발송 첫 실행 시 Firestore 복합 색인이 필요할 수 있음(오류 로그의 링크로 생성)

자세한 배경·전체 체크리스트는 `docs/product-specs/admin-push.md` 참고.

### 미완성 — 다음에 이어서 해야 할 것
1. **한국 공휴일 데이터는 2025~2028년까지만 등록됨** (`data/kr-holidays.js`) — 2029년 이후는 매년 말 다음 해 공휴일이 확정되면 추가해야 함. 2028년은 설날·추석 등 음력 날짜는 맞지만 대체공휴일 일부는 아직 확정 고시 전이라 보수적으로 비워둔 상태
2. **FCM 진짜 푸시 알림** — 코드는 1·2단계(수신 인프라+자동 발송) 전부 완료(v0.0.36~38). 위 "⚠️ 배포 필요" 항목만 남음
3. **성장 예측은 아주 단순한 선형 추정**임 — WHO 성장 곡선 기반 예측처럼 정교하게 하려면 추가 작업 필요
4. **다크 모드가 앱 전체 하드코딩 색상을 100% 커버하진 못했을 수 있음** — 매번 새로 발견되는 걸 보면 아직 못 찾은 구석이 남아있을 가능성이 높음
5. **체크리스트 항목 전면 추가/수정 예정** — 옹짐꾼님이 세부 내용(어느 카테고리, 어떤 항목) 주시면 진행
6. **관리자 전용 푸시 발송(v0.0.39)** — 코드는 완료, 위 "⚠️ 배포 필요(v0.0.39)" 항목만 남음
7. **체크리스트 커스터마이징(v0.0.40)** — 코드는 완료. guide 육아정보 페이지에 준비물 팩 4종 콘텐츠 미반영, 사용자 정의 체크리스트 항목 통째 편집 UI 없음(아래 "다음 후보" 참고)

### momcal.vercel.app 서비스워커 이슈 — "지금 방식 유지"로 결정됨
momcal.vercel.app 접속자가 거의 없어서, 301 리다이렉트(momcal.vercel.app → momcal.app)는 그대로 유지하기로 결정. 참고로 momcal.vercel.app에 예전에 설치된 서비스워커는 리다이렉트 때문에 더 이상 자체 업데이트를 받을 수 없어 영구적으로 예전 버전에 머무름 — 코드로 고칠 수 없고 해당 사용자가 브라우저 사이트 데이터를 지워야만 해결됨. 감수하기로 함.

### 알아두면 좋은 것
- 정식 도메인은 `momcal.app` — `momcal.vercel.app`도 301 리다이렉트로 계속 접속은 가능(단, 서비스워커 이슈로 예전 방문자는 갱신 안 될 수 있음, 감수하기로 결정됨)
- 폰트 2종 체제: 제목/본문/버튼/로고는 "온글잎 박다현체", "정보 글"은 "오뮤 다예쁨체" — 둘 다 `font-weight: normal` 고정
- 하단 탭: 홈/캘린더/체크리스트/성장/설정 5개, "설정" 탭만 폭이 좁게 디자인됨
- 체크리스트 실시간 동기화(onSnapshot) 관련 코드를 만질 땐 `js/state.js`의 `subscribeToUserData` 주석 참고 — `hasPendingWrites`로 "내 저장 에코"와 "실제 원격 변경"을 구분해야 화면이 불필요하게 중복 렌더링되지 않음
- 캘린더 주간 뷰는 `S.calWeekRef`(YYYY-MM-DD) 자체 기준일을 갖고, `calMove()`가 뷰에 따라 분기(월간=월 단위, 주간=7일 단위)해서 이동시킴 (`js/calendar.js`)
- 캘린더 셀에서 이유식 스티커(`FOOD_STICKER_SET`)는 다른 스티커의 "+N" 카운트와 완전히 분리되어 날짜 숫자 옆에 표시됨
- 캘린더 토·일·공휴일 날짜는 `--holiday-red` 변수로 붉은색 표시(`data/kr-holidays.js`로 공휴일 판별) — 달력 셀에는 이름을 안 넣고, 날짜를 눌러 뜨는 세부일정 패널 상단에서만 공휴일 이름을 보여줌(v0.0.24, `showDayPanel`)
- **가족 그룹 공유(v0.0.12, v0.0.15에서 실사용 확인됨)**: `users/{uid}.familyId`가 있으면 `families/{familyId}` 문서를 데이터 소스로 씀. 자세한 구조는 `docs/product-specs/family-sharing.md` 참고
- **체크리스트 탭은 v0.0.40부터 key 기반**(`js/checklist.js`의 `getVisibleTabDefs`) — `S.clTab===0`처럼 숫자 위치로 분기하는 코드를 새로 추가하지 말 것(탭 숨김 기능 때문에 위치가 고정이 아님). 새 준비물 팩을 추가하려면 `data/checklist-packs.js`에 객체만 추가하면 됨 — 렌더링 코드는 안 건드려도 됨
- "내 일정"(custom)으로 직접 추가하는 일정은 색상을 파스텔 스와치 7개 중 골라 저장함(`ev.color` 필드, `getEvDisplayColor()`가 우선 적용) — 그 외 종류(필수/추천/접종/정부지원)는 캘린더 하단 범례에서 카테고리 공통 색을 바꿈
- 캘린더 안 일정 글자(.ev-line/.week-ev-block 등, px 고정이라 앱 전체 글자 크기 설정의 영향을 안 받음)만 따로 조절하는 설정이 있음(`js/calFontSize.js`, `data-cal-fontsize` 속성) — 설정 탭 + 캘린더 툴바 설정 아이콘에서 접근 가능
- **v0.0.18**: 저장소 문서 구조를 `AGENTS.md`/`ARCHITECTURE.md`/`docs/`로 재정비(자세한 내용은 CHANGELOG.md v0.0.18 참고). 문서를 새로 추가하고 싶을 땐 먼저 `AGENTS.md`의 "문서 간 역할 분리" 표를 확인해서 중복 문서를 만들지 말 것
- 더 오래된 버전별 세부 변경 이력은 이 문서에서 지웠습니다 — 필요하면 `docs/CHANGELOG.md`에서 버전 번호로 찾으세요(코드 안 주석에도 대부분 남아있음)
- **⚠️ v0.0.38 배포 직후 "아이 2명 이상일 때 전환 안 됨·캘린더 먹통·등록/삭제 안 됨" 증상 보고됨 → 원인은 코드 버그가 아니라 브라우저에 남아있던 예전 서비스워커/캐시였음**. 컴퓨터를 완전히 껐다 켜니 해결됨(하드 리프레시 `Ctrl+Shift+R`나 F12→Application→Service Workers→Unregister로도 해결 가능). 새 버전 배포 직후 "방금까지 잘 되던 기본 기능이 갑자기 안 된다"는 보고가 오면, 코드부터 의심하기 전에 캐시 문제 가능성을 먼저 안내할 것 — 특히 로그인 계정에서만 재현되고 게스트 모드는 멀쩡하다는 보고는 데이터 소스 차이(캐시된 구버전 코드가 Firestore 데이터 구조와 안 맞아서 생기는 착시)일 수 있음을 염두에 둘 것

### 계속 지켜야 할 원칙 (매번 리마인드됨)
- 기존 기능 절대 삭제 금지, 새 프로젝트 생성 금지, Firebase 구조 유지(새 컬렉션·필드 추가는 OK, 기존 구조는 안 건드림), 기존 UI 최대한 유지, 모든 기능 모듈화, 복잡한 기능보다 사용성 우선, "부모가 매일 여는 앱" 목표
- **버전을 올릴 때마다 `index.html`의 `.site-footer-version` + `scripts/build-guide.mjs`의 `APP_VERSION` 두 곳을 함께 올리고 `node scripts/build-guide.mjs` 재실행** (v0.0.15~16에서 빼먹었던 실수 — PROJECT_SPEC.md "버전 관리 정책" 참고)

---

## 현재 확인 필요 항목

### v0.0.40 (이번 버전 — 체크리스트 커스터마이징)
- [ ] 체크리스트 탭에서 예방접종/발달/이유식이 기존과 똑같이 잘 보이는지 확인 (탭 판별 로직을 리팩터링했어서 회귀 여부가 특히 중요함)
- [ ] 육아 단계 아이 계정에서 "외출 준비물/100일 준비/돌 준비/돌사진 준비" 탭이 보이는지, 체크·진행률이 정상 동작하는지 확인
- [ ] 설정 탭 → "체크리스트 관리"에서 탭 숨기기 → 체크리스트 탭에서 실제로 사라지는지, 다시 표시로 되돌리면 복구되는지 확인
- [ ] 설정 탭에서 "＋ 새 체크리스트 만들기"로 만든 커스텀 체크리스트가 체크리스트 탭에 나타나는지, 체크·삭제가 정상 동작하는지 확인
- [ ] 예방접종 탭 캘린더 연동을 꺼본 뒤, 체크리스트에서 접종을 체크해도 캘린더 쪽 완료 표시가 안 바뀌는지 확인(다시 켜면 정상 연동되는지도)
- [ ] 화면 최하단 버전 표시가 "v0.0.40"으로 보이는지 확인

### v0.0.39 (지난 버전 — 관리자 전용 푸시 발송)
- [ ] Firebase 콘솔에서 `adminBroadcasts` Firestore 보안 규칙 추가 (`docs/product-specs/admin-push.md` 참고)
- [ ] `functions/scripts/set-admin-claim.cjs`로 관리자 계정에 클레임 부여 후 재로그인해서 `/admin.html` 접근 확인
- [ ] `cd functions && npm run deploy`로 `onBroadcastCreated`/`processScheduledBroadcasts` 배포
- [ ] 전체 대상 즉시 발송 1건 테스트 → 발송 이력에 `발송완료`와 성공/실패 수 표시되는지 확인
- [ ] 예약 발송 1건 테스트 → 예약 시각 5분 이내에 발송되는지 확인(색인 생성 안내가 뜨면 링크로 생성)

### 참고 — 알아두면 좋은 것
- **v0.0.29부터**: "지난 버전 확인 항목"은 바로 이전 버전 것만 남기고 그 이전 버전들은 이 파일에서 지움(이미 CHANGELOG.md에 다 기록돼 있어서 중복이었음) — 새 버전이 나오면 이번 버전 항목은 위로, 그 전 "지난 버전" 항목은 지우는 방식으로 계속 유지
- **v0.0.30부터**: `clData.born`이 `clData.born_vax`(예방접종)/`clData.born_dev`(발달)로 나뉨 — 새 항목 추가 시 두 배열이 같은 catKey를 공유한다는 점, id 접두어를 겹치지 않게 써야 한다는 점을 `data/checklist-data.js` 상단 주석과 CHANGELOG.md v0.0.30 항목에서 꼭 확인할 것
- **v0.0.31부터**: 이유식은 캘린더 자동 일정이 아니라 스티커(`emoji|grams` 형식)로 기록함 — `data/milestones.js`에 `foodEvs`는 더 이상 없음. **v0.0.32부터**: 이유식 필터는 이벤트를 거르는 게 아니라 셀 가운데에 이유식 스티커를 강조해서 보여주는 방식으로 동작함(`applyCalFilter`가 `f.food`일 때 이벤트를 전부 숨기고, `cellHTML`/`renderWeekView`가 `day-food-center`를 그림) — 필터는 이제 배타적 선택(한 번에 하나만)임
- **`guide/` 페이지는 `data/checklist-data.js`나 `data/government-support.js`를 수정하면 자동으로 갱신되지 않음** — `node scripts/build-guide.mjs`를 다시 실행해야 반영됨
- **`data/government-support.js`의 `GOV_INFO_BASIS`는 제도 내용이 바뀔 때마다 함께 갱신해야 함**
- **`data/kr-holidays.js`는 매년 말 다음 해 공휴일이 확정되면 함께 갱신해야 함**
- **정적 파일(아이콘·CSS·JS) 내용을 바꾸면 `sw.js`의 `CACHE_NAME` 버전도 함께 올려야 기존 사용자에게 반영됨** (현재 v45) — 새 `js/` 파일을 추가할 땐 `APP_SHELL` 배열에도 추가할 것(v0.0.18에서 8개 누락분 발견·수정함). 단, `admin.html`/`js/admin.js`/`css/admin.css`처럼 `privacy.html`류 독립 페이지에 속하는 파일은 `APP_SHELL`에 넣지 않는 게 기존 관례(v0.0.39)
- **버전을 올릴 땐 `index.html`의 `.site-footer-version`과 `scripts/build-guide.mjs`의 `APP_VERSION`도 반드시 함께 올리고 `node scripts/build-guide.mjs`를 재실행할 것** (v0.0.15~16에서 빼먹었던 항목 — 자세한 사고 경위는 CHANGELOG.md v0.0.17 참고)
- **AdSense 실제 신청·심사는 진행하지 않음** — 정책 페이지만 준비해둔 상태

---

## 다음 후보 (미정 — 우선순위 논의 필요)

> v0.0.16부터 "고민 중" 항목도 이 섹션에서 함께 관리합니다.

- [ ] **홈 화면에 "오늘 해야 할 일" 요약 추가** — 오늘 날짜 기준 캘린더 일정·체크리스트 미완료 필수 항목을 한 곳에 모아 보여주는 카드. 지금은 "다음 일정" 카드가 있지만 "오늘 안에 뭘 해야 하는지"를 한눈에 모아 보여주진 않음
- [ ] **이모지 대신 실제 그림 파일(이미지 에셋) 추가** — 아이 프로필 아바타(아기 남아/여아 등, 옹짐꾼님이 예시로 주신 손그림 스타일 참고), 체크리스트 성장 단계(씨앗→나무→아이) 등을 유니코드 이모지 대신 실제 PNG/SVG 일러스트로 교체. Claude에게는 이미지 생성 도구가 없어서 실제 그림 파일을 옹짐꾼님이 만들어서 주셔야 진행 가능 — 받으면 `icons/` 폴더에 정식 에셋으로 추가
- [ ] **하드코딩된 파스텔 배경색 잔여분 그레이스케일 정리** (v0.0.20~21에서 대부분 정리했지만, 카드 hover 등 새로 발견되는 곳이 있으면 `docs/UI_GUIDELINE.md` 색상 예외 목록 기준으로 판단해서 정리)
- [ ] **체크리스트 항목 전면 추가/수정** — 옹짐꾼님이 세부 내용 주시면 진행
- [ ] **육아정보(guide/) 페이지에 준비물 팩 4종 콘텐츠 추가** — 체크리스트 쪽엔 외출 준비물/100일 준비/돌 준비/돌사진 준비가 생겼는데(v0.0.40), guide 페이지엔 아직 대응 콘텐츠가 없음
- [ ] **사용자 정의 체크리스트 항목 편집 UI** — 지금은 만든 뒤 항목을 통째로 바꿀 수 없고(개별 추가/삭제만 가능), 체크리스트 자체를 지우고 다시 만들어야 함
- [ ] **준비물 팩을 임신 단계에도 노출** — 지금은 born 단계 전용(`js/checklist.js`의 `packTabDefs()`), 임신 단계용 팩이 생기면 `stage` 필드로 분기 확장 필요
- [ ] 병원 방문 기록 상세 (진료과·의사 메모 등)
- [ ] AI 육아비서 구조 (OpenAI 연결 준비)
- [ ] 원더윅스(Wonder Weeks) 기간을 캘린더/체크리스트에 추가하는 것 고려
- [ ] 캘린더 앱인 만큼 첫 화면(홈)에 캘린더 자체가 보여야 하지 않을지 고민
- [ ] 임신 체크리스트 주차별 세분화 분류 (필수/추천/정부지원/병원/준비물/육아팁)
- [ ] 정부지원 항목별 정확한 신청 링크(딥링크)로 교체
- [ ] 제도 정보(금액·자격 요건) 연 1회 이상 최신화 필요

---

## 버그

> 발견된 버그는 여기에 추가하세요.

- (현재 없음)
