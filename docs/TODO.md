# TODO

> **Claude에게**: 이 파일에 있는 작업만 수행하세요.
> 완료한 항목은 이 파일에서 삭제하고 CHANGELOG.md에 기록하세요.
> PROJECT_SPEC.md와 UI_GUIDELINE.md를 먼저 읽고 시작하세요.
>
> **이 문서의 역할**: "지금 무엇을 해야 하는지"만 다룹니다 — 현재 버전 확인 항목, 다음 후보 기능, 버그.
> 프로젝트 구조·스키마·완료된 기능 이력 같은 참고 정보는 여기 적지 말고 **PROJECT_SPEC.md**에 적어주세요.
> 지난 버전의 확인 항목은 배포·확인이 끝나면 이 파일에서 지웁니다 (기록은 CHANGELOG.md에 이미 남아있음).
> **v0.0.2부터**: "스프린트"라는 용어 대신 "버전"(v0.0.x)으로 통일해서 부릅니다 (Sprint 1~28은 버전 체계 도입 이전 기록이라 그대로 둠, Sprint 29 = v0.0.1).
> **v0.0.16부터**: "고민 중"(결정 안 된 아이디어)과 "다음 후보"(우선순위 미정 기능)를 하나로 합쳐 **"다음 후보"** 한 섹션에서만 관리합니다.

---

## ⚠️ 새 대화로 이어갈 때 먼저 읽어주세요 (v0.0.26 인계 노트)

지금까지의 자세한 변경 이력은 CHANGELOG.md에 버전별로 다 남아있고, PROJECT_SPEC.md·UI_GUIDELINE.md는 항상 최신 상태로 맞춰뒀으니 이 파일들을 먼저 읽으면 맥락 파악이 될 거예요.

### 🆕 v0.0.24 — 공휴일 이름 표시, 성장 백분위 WHO LMS 정교화
- **공휴일 이름**: 달력 셀에는 여전히 이름을 안 넣고(스티커와 자리 겹침) 날짜 색만 바꿈. 대신 날짜를 눌러 뜨는 세부일정 패널 상단에 공휴일 이름을 표시함(`js/calendar.js`의 `showDayPanel` + `getHoliday()`).
- **성장 백분위 WHO LMS화**: 백분위 계산을 정규분포 근사에서 **WHO LMS(Box-Cox) 공식**으로 바꿨음. `data/who-growth.js`가 이제 `{m, L, M, S}` 구조임 — 성장 계산 코드를 만질 땐 이 구조를 전제로 볼 것.
  - 키·머리둘레는 WHO 표준상 L=1이라 기존과 사실상 동일하게 동작하고, 몸무게만 비대칭(L·S)이 실제로 반영됨.
  - 참고선은 P3/P15/P50/P85/P97 5개(임상 성장도표 표준).
  - ⚠️ **이 LMS 표는 대표 월령 anchor 값 + 선형 보간**임. 더 정밀하게 하려면 WHO 공식 0~60개월 1개월 단위 CSV를 그대로 넣으면 되고, 그때 `data/who-growth.js`의 표 행만 교체하면 `js/growthChart.js`는 안 건드려도 되도록 구조를 맞춰뒀음. \"아무 표에서나 가져오지 말 것\" 원칙은 여기에도 그대로 적용 — 넣으려면 WHO 공식 CSV 단일 출처로.
  - 태아(임신 중) 성장 화면은 그대로(원래 WHO 백분위 비교를 안 함).

### 🆕 v0.0.23 — 임신 중 태아 성장 기록, 체크리스트 도움돼요/아쉬워요, 이미지 공유
- **태아 성장 기록**: 임신 중인 아이도 이제 성장(그래프) 탭에서 기록을 남길 수 있음(임신 주수·추정 체중g·태아 길이cm). `growthRecords`에 `week`/`isFetal` 필드가 새로 생김 — 이 두 필드로 "출생 후 기록"과 "태아 기록"을 구분하니, 성장 관련 코드를 만질 때는 항상 `isFetal` 여부를 먼저 확인할 것. **의도적으로 WHO 백분위 비교·"한 달 뒤 예상" 예측은 태아 기록엔 안 넣었음**(출처마다 태아 기준치가 크게 달라 정확성을 보장할 수 없어서) — 나중에 이 부분을 요청받아도, 신뢰할 수 있는 단일 출처(예: WHO/INTERGROWTH-21st) 없이 그냥 아무 표에서나 가져오지 말 것
- **체크리스트 도움돼요/아쉬워요**: 새 Firestore 필드 `itemFeedback`은 "여러 사용자가 집계하는 공개 투표"가 **아니라** 개인 반응임 — 공개 집계를 만들려면 새 공개 Firestore 컬렉션 + 보안 규칙(콘솔 작업)이 필요해서 이번엔 범위를 좁혔음. "실제 인기 투표를 보여달라"는 요청이 오면 이 설계 차이를 먼저 안내할 것
- **체크리스트 이미지 공유**: `html2canvas`를 CDN으로 새로 추가함(Chart.js와 동일한 폴백 패턴) — 화면을 그대로 캡처하지 않고 공유 전용으로 만든 카드를 캡처함

### 🔧 v0.0.22 — "데이터 포맷이라 못 바꾼다"던 예방접종·정부지원 마커, 실제로 리팩터링해서 해결함
지난 버전에서 예방접종(💉)·정부지원(🟢) 이벤트 제목의 이모지 접두어를 "건드리면 여러 기능이 깨지는 데이터 포맷"이라고 안내했었는데, 실제로 뜯어보니 리팩터링으로 해결 가능했어요:
- `getAutoEvs()`가 제목 앞에 이모지를 붙이던 걸 없애고, `getEventKey()`·`vaccineSeries.js`·`checklistCalendarLink.js` 전부 문자열 매칭 대신 이미 있던 `ev.type` 필드로 판별하도록 바꿈
- `js/state.js`의 `migrateEventModKeys()`가 데이터 로드 시점마다 예전 이모지 포함 키를 새 키로 자동 이전 — 기존 사용자의 접종 완료 기록 등이 끊기지 않음
- **교훈**: "지금 당장은 안전하게 못 바꾼다"는 판단이 "영원히 못 바꾼다"는 뜻은 아님 — 마이그레이션 경로를 설계하면 풀리는 경우가 있으니, 다음에 비슷하게 "데이터 의존이라 못 건드림" 판단을 내릴 때는 정말 방법이 없는지 한 번 더 검토할 것

### 🎨 나머지 반영 사항
- Legend 배지가 아이콘 없이 비어 보이던 문제 → 트로피 아이콘 + 200% 표시로 수정
- 정부지원 사이드바 안내 문구의 어색한 줄바꿈 정리(수동 `<br>` 제거)
- 체크리스트 사이드바에서 라벨이 길 때 배지가 밀리던 레이아웃 정리
- 일정 종류 선택(필수/추천)의 세로 정렬 불일치 → 아이콘으로 통일해서 해결
- **그라디언트 완전 제거** — 마지막까지 남아있던 메인 버튼(`.bpk`) 그라디언트도 단색으로 바꿔서, 이제 앱 전체에 버튼용 그라디언트가 없음(배지류만 예외)
- 육아 체크리스트 "아기→어린이" 단계 아이콘이 아이 성별(남아/여아)에 따라 바뀜(중간 단계만 — Unicode에 "아기 남아/여아" 전용 이모지가 없어서 첫/마지막 단계는 중성 유지)
- ⚠️ 옹짐꾼님이 참고 이미지로 주신 손그림 스타일 캐릭터 6종은 실제로 만들지 못했음 — 이미지 생성 도구가 없어서 이모지로 대체함. 실제 그림 파일을 만들어서 주시면 정식 에셋으로 넣을 수 있음

### 🎨 v0.0.20~21 — "AI스럽지 않은" 디자인 리뉴얼 + 아이콘 마이그레이션
`docs/UI_GUIDELINE.md`에 디자인 철학·색상 예외 목록·아이콘 시스템 섹션이 있고, 대부분의 UI 크롬이 Material Symbols 아이콘으로 전환되어 있어요. 새 UI를 만들 때는 이 문서를 먼저 참고하세요.

### 🚫 v0.0.19 — `AGENTS.md`에 "금지 규칙" 섹션이 새로 생겼습니다
"최우선 원칙" 바로 아래에 실제로 반복됐던 실수 7가지 + 이 저장소에서 절대 건드리면 안 되는 파일·값 5가지를 정리해뒀어요. 새 세션 시작할 때 `AGENTS.md`를 읽으면 자동으로 보이지만, 특히 `guide/*.html` 직접 수정 금지·`js/firebase.js` 설정값 변경 금지처럼 "왜 안 되는지 모르고 건드리기 쉬운 것"들이니 한 번은 꼭 훑어보세요.

### 📁 v0.0.18 — 저장소 구조가 바뀌었습니다 (하네스 엔지니어링 대비)
루트에 있던 `PROJECT_SPEC.md`/`UI_GUIDELINE.md`/`TODO.md`(이 파일)/`CHANGELOG.md`가 전부 `docs/` 아래로 이동했습니다. 그리고 루트에 새로 두 파일이 생겼어요:
- **`AGENTS.md`** — AI 코딩 에이전트가 가장 먼저 읽는 진입점(원칙·워크플로·체크리스트). 이 TODO.md의 "최우선 원칙"은 이제 여기가 원본입니다.
- **`ARCHITECTURE.md`** — 기술 스택·디렉터리 구조·모듈 맵·Firebase 개요·전역 상태(S)·이벤트 타입 등 "어떻게 만들어졌는지"를 다룸. `docs/PROJECT_SPEC.md`에 있던 "기술 스택"/"프로젝트 구조"/"전역 상태 객체"/"이벤트 타입"/"핵심 개발 원칙" 섹션이 여기로 옮겨가고 `docs/PROJECT_SPEC.md`엔 포인터만 남았습니다.

새 세션에서 작업을 시작하기 전엔 **`AGENTS.md`를 가장 먼저** 읽어주세요.

### 🚨 v0.0.15~16에서 화면 버전 표시를 안 올렸던 사고 — v0.0.17에서 수정
PROJECT_SPEC.md "버전 관리 정책"에 버전을 올릴 때마다 `index.html`의 `.site-footer-version`과 `scripts/build-guide.mjs`의 `APP_VERSION` 두 곳을 **반드시 함께** 수정하라고 명시돼 있는데도, v0.0.15·v0.0.16 두 버전 모두 CHANGELOG/TODO/캐시 버전만 올리고 이 두 곳을 빼먹어서 화면에는 계속 "v0.0.14"로 보였음. 원인은 두 버전 다 "이번 버전에서 무엇을 바꿀지" 체크리스트에만 집중하고 PROJECT_SPEC.md의 버전 관리 정책 항목을 다시 확인하지 않은 단순 누락. v0.0.17에서 두 곳 모두 "v0.0.17"로 맞추고 `node scripts/build-guide.mjs`를 재실행해 `guide/*.html`에도 반영함. **앞으로 버전을 올릴 때는 이 두 곳을 체크리스트 맨 앞에 놓고 빼먹지 않을 것.** (이제 이 체크리스트는 `AGENTS.md`의 "작업 완료 후 체크리스트"가 1차 출처입니다.)

### 미완성 — 다음에 이어서 해야 할 것
1. **한국 공휴일 데이터는 2025~2028년까지만 등록됨** (`data/kr-holidays.js`) — 2029년 이후는 매년 말 다음 해 공휴일이 확정되면 추가해야 함. 2028년은 설날·추석 등 음력 날짜는 맞지만 대체공휴일 일부는 아직 확정 고시 전이라 보수적으로 비워둔 상태
2. **FCM 진짜 푸시 알림** — 아직 "앱을 열었을 때 확인하는" 방식(`js/notifications.js`)뿐, 앱을 꺼도 오는 진짜 푸시는 아님. 완성하려면 옹짐꾼님이 Firebase 콘솔에서 직접 해야 하는 일(Cloud Messaging 사용 설정+VAPID 키 발급, Blaze 요금제 전환)이 있음
3. **성장 예측은 아주 단순한 선형 추정**임 — WHO 성장 곡선 기반 예측처럼 정교하게 하려면 추가 작업 필요
4. **다크 모드가 앱 전체 하드코딩 색상을 100% 커버하진 못했을 수 있음** — 매번 새로 발견되는 걸 보면 아직 못 찾은 구석이 남아있을 가능성이 높음
5. **체크리스트 항목 전면 추가/수정 예정** — 옹짐꾼님이 세부 내용(어느 카테고리, 어떤 항목) 주시면 진행

### momcal.vercel.app 서비스워커 이슈 — "지금 방식 유지"로 결정됨
momcal.vercel.app 접속자가 거의 없어서, 301 리다이렉트(momcal.vercel.app → momcal.app)는 그대로 유지하기로 결정. 참고로 momcal.vercel.app에 예전에 설치된 서비스워커는 리다이렉트 때문에 더 이상 자체 업데이트를 받을 수 없어 영구적으로 예전 버전에 머무름 — 코드로 고칠 수 없고 해당 사용자가 브라우저 사이트 데이터를 지워야만 해결됨. 감수하기로 함.

### 알아두면 좋은 것
- 정식 도메인은 `momcal.app` — `momcal.vercel.app`도 301 리다이렉트로 계속 접속은 가능(단, 서비스워커 이슈로 예전 방문자는 갱신 안 될 수 있음, 감수하기로 결정됨)
- 폰트 2종 체제: 제목/본문/버튼/로고는 "온글잎 박다현체", "정보 글"은 "오뮤 다예쁨체" — 둘 다 `font-weight: normal` 고정
- 하단 탭: 홈/캘린더/체크리스트/성장/설정 5개, "설정" 탭만 폭이 좁게 디자인됨
- 체크리스트 실시간 동기화(onSnapshot) 관련 코드를 만질 땐 `js/state.js`의 `subscribeToUserData` 주석 참고 — `hasPendingWrites`로 "내 저장 에코"와 "실제 원격 변경"을 구분해야 화면이 불필요하게 중복 렌더링되지 않음
- 캘린더 주간 뷰는 `S.calWeekRef`(YYYY-MM-DD) 자체 기준일을 갖고, `calMove()`가 뷰에 따라 분기(월간=월 단위, 주간=7일 단위)해서 이동시킴 (`js/calendar.js`)
- 캘린더 셀에서 이유식 스티커(`FOOD_STICKER_SET`)는 다른 스티커의 "+N" 카운트와 완전히 분리되어 날짜 숫자 옆에 표시됨
- 캘린더 토·일·공휴일 날짜는 `--holiday-red` 변수로 붉은색 표시(`data/kr-holidays.js`로 공휴일 판별) — 공휴일 이름 자체는 화면에 안 띄움(아래 "다음 후보" 참고)
- **가족 그룹 공유(v0.0.12, v0.0.15에서 실사용 확인됨)**: `users/{uid}.familyId`가 있으면 `families/{familyId}` 문서를 데이터 소스로 씀. 자세한 구조는 `docs/product-specs/family-sharing.md` 참고
- "내 일정"(custom)으로 직접 추가하는 일정은 색상을 파스텔 스와치 7개 중 골라 저장함(`ev.color` 필드, `getEvDisplayColor()`가 우선 적용) — 그 외 종류(필수/추천/접종/정부지원)는 캘린더 하단 범례에서 카테고리 공통 색을 바꿈
- 캘린더 안 일정 글자(.ev-line/.week-ev-block 등, px 고정이라 앱 전체 글자 크기 설정의 영향을 안 받음)만 따로 조절하는 설정이 있음(`js/calFontSize.js`, `data-cal-fontsize` 속성) — 설정 탭 + 캘린더 툴바 설정 아이콘에서 접근 가능
- **v0.0.18**: 저장소 문서 구조를 `AGENTS.md`/`ARCHITECTURE.md`/`docs/`로 재정비(위 인계 노트 참고). 문서를 새로 추가하고 싶을 땐 먼저 `AGENTS.md`의 "문서 간 역할 분리" 표를 확인해서 중복 문서를 만들지 말 것
- 더 오래된 버전별 세부 변경 이력은 이 문서에서 지웠습니다 — 필요하면 `docs/CHANGELOG.md`에서 버전 번호로 찾으세요(코드 안 주석에도 대부분 남아있음)

### 계속 지켜야 할 원칙 (매번 리마인드됨)
- 기존 기능 절대 삭제 금지, 새 프로젝트 생성 금지, Firebase 구조 유지(새 컬렉션·필드 추가는 OK, 기존 구조는 안 건드림), 기존 UI 최대한 유지, 모든 기능 모듈화, 복잡한 기능보다 사용성 우선, "부모가 매일 여는 앱" 목표
- **버전을 올릴 때마다 `index.html`의 `.site-footer-version` + `scripts/build-guide.mjs`의 `APP_VERSION` 두 곳을 함께 올리고 `node scripts/build-guide.mjs` 재실행** (v0.0.15~16에서 빼먹었던 실수 — PROJECT_SPEC.md "버전 관리 정책" 참고)

---

## 현재 확인 필요 항목

### v0.0.26 (이번 버전 — guide 그라디언트 제거 + footer 통일)
- [ ] 화면 최하단 버전 표시가 "v0.0.26"으로 보이는지 확인
- [ ] 육아정보 페이지의 CTA 배너·헤더 버튼·히어로 배너가 핑크 단색으로 표시되는지 확인 (보라/그라디언트 없음)
- [ ] 개인정보처리방침·이용약관·문의 페이지 하단 footer가 아이콘 링크 4개 형태로 정상 표시되는지 확인

### v0.0.25 (지난 버전 — 버그 6종 수정)
- [ ] 화면 최하단 버전 표시가 "v0.0.25"로 보이는지 확인
- [ ] 성장 탭에서 임신 중 아이와 출생 후 아이를 번갈아 선택해도 화면이 겹치지 않는지 확인
- [ ] 성장 기록 추가 시 그래프가 한 번만 갱신되는지 확인(두 번 깜빡임 없음)
- [ ] 육아정보 페이지 헤더의 CTA 버튼이 "맘캘 앱 열기"로 표시되는지, footer 링크가 4개인지 확인
- [ ] 월간에서 주간 뷰로 전환 시 오늘 날짜가 포함된 주차로 이동하는지 확인
- [ ] 체크리스트 이유식 탭 사이드바에서 "6개월"과 "(초기 1단계)"가 줄바꿈되어 각각 한 줄로 표시되는지 확인

### v0.0.24 (지난 버전 — 공휴일 이름 표시, 성장 백분위 WHO LMS 정교화)
- [ ] 화면 최하단 버전 표시가 "v0.0.24"로 보이는지 확인 (앱 본체 + 육아정보 + 정책 페이지 전부)
- [ ] **새 기능**: 캘린더에서 공휴일(예: 설날·추석·어린이날)을 눌렀을 때 세부일정 패널 상단에 공휴일 이름이 붉은 배지로 뜨는지 확인 — 공휴일이 아닌 날엔 안 뜨는지도 확인
- [ ] **새 기능**: 성장(그래프) 탭에서 참고선이 5개(P3/P15/P50/P85/P97)로 보이는지, 몸무게·키·머리둘레 전환이 정상인지 확인. 아이 키/몸무게를 또래 평균쯤으로 입력하면 백분위가 50% 근처로 나오는지 확인
- [ ] (선택) 몸무게 백분위가 예전보다 자연스러워졌는지 — 특히 아주 크거나 작은 값을 넣었을 때 상·하위 백분위가 실제 성장곡선처럼 나오는지 확인

### v0.0.23 (지난 버전 — 정부지원 사이드바 통일, 태아 성장 기록, 체크리스트 추천/비추천, 이미지 공유)
- [ ] 체크리스트 정부지원 탭의 "육아정보 더 알아보기" 박스가 다른 탭과 똑같은 여백으로 보이는지 확인
- [ ] 체크리스트 정부지원 탭의 "육아정보 더 알아보기" 박스가 다른 탭과 똑같은 여백으로 보이는지 확인
- [ ] **새 기능**: 임신 중인 아이를 선택한 상태에서 성장(그래프) 탭에 들어가면 "임신 주수·추정 체중(g)·태아 길이(cm)" 입력 화면이 나오는지, 기록을 추가하면 그래프에 점이 찍히는지 확인
- [ ] 홈 대시보드에서 임신 중인 아이의 성장 카드가 "태아 기록"으로 표시되고 g/cm 단위가 올바른지 확인 (kg으로 잘못 나오면 버그)
- [ ] **새 기능**: 체크리스트 항목 상세 설명을 펼치면 "도움돼요/아쉬워요" 버튼이 보이는지, 눌렀을 때 색이 바뀌는지(다시 누르면 취소되는지) 확인 — `guide/` 육아정보 페이지의 같은 항목에도 버튼이 있는지 확인(단, 앱과 육아정보 페이지의 반응은 서로 연동되지 않고 각자 따로 저장됨 — 정상 동작)
- [ ] **새 기능**: 체크리스트 화면 우측 상단 공유 아이콘을 눌렀을 때 이미지가 생성되는지(모바일에서는 공유 시트, PC에서는 파일 다운로드) 확인 — 이미지 안에 진행률·완료 항목·"momcal.app" 표시가 잘 보이는지 확인. 인터넷이 느리면 html2canvas 로딩 때문에 첫 시도는 안내 문구가 뜨고 재시도해야 할 수 있음(정상)

### v0.0.22 — 지난 버전 확인 항목 (아직 확인 전)
- [ ] **중요**: 예방접종 캘린더 일정 중 이미 "완료" 체크해둔 게 있다면, 이번 업데이트 후에도 완료 상태·실접종일·병원명이 그대로 남아있는지 꼭 확인해주세요(제목의 이모지 마커를 없애는 리팩터링이 있었어서, 기존 데이터가 안 끊겼는지 확인이 특히 중요함)
- [ ] 정부지원 신청 상태(신청완료/지급완료로 표시해둔 것)도 이전과 동일하게 남아있는지 확인
- [ ] 체크리스트 등급 배지가 Perfect/Master/Legend 전부 아이콘과 함께 잘 보이는지, Legend는 "200%"로 표시되는지 확인
- [ ] 일정 추가 화면에서 "필수"·"추천" 아이콘이 다른 버튼들과 세로로 잘 맞는지, "일정 추가" 버튼이 그라디언트 없이 단색 핑크로 보이는지 확인
- [ ] 남아/여아로 등록한 아이의 체크리스트 "어린이(24~35개월)" 단계 아이콘이 성별에 맞게 보이는지 확인

### v0.0.21 — 지난 버전 확인 항목 (아직 확인 전)
- [ ] 체크리스트 임신 탭에서 각 주차 카테고리 옆에 과일 이모지(블루베리→포도→키위→...→수박)가 순서대로 보이는지 확인 — `guide/pregnancy.html`에도 동일하게 반영됐는지 확인
- [ ] 체크리스트 육아 탭에서 각 개월 카테고리 옆에 씨앗→새싹→나무→아기→어린이로 이어지는 이모지가 보이는지 확인 — `guide/parenting.html`에도 동일하게 반영됐는지 확인
- [ ] 캘린더에서 일정 클릭 → 수정 모달, 데이 패널, 스티커 탭에 이모지 대신 아이콘이 잘 보이는지 확인

### v0.0.20 — 지난 버전 확인 항목 (아직 확인 전)
- [ ] 하단 내비게이션(홈/캘린더/체크리스트/성장/설정), 상단 등록/로그인 버튼에 이모지 대신 아이콘이 잘 보이는지 확인 — 아이콘이 안 보이고 네모(□)나 글자로 깨져 보이면 Material Symbols 폰트 로딩 문제일 수 있음(네트워크 확인 필요)
- [ ] 홈 대시보드 카드 아이콘이 회색 배경 + 핑크 아이콘으로 통일되어 보이는지 확인 (이전엔 카드마다 다른 파스텔 배경)
- [ ] 전체적으로 버튼 모서리가 이전보다 약간 각져 보이는지(반경 16px) 확인
- [ ] 다크 모드에서도 위 사항들이 이상 없이 보이는지 확인(새 회색 변수 다크 대응 확인용)
- [ ] `docs/product-specs/` 폴더가 잘 있고 `docs/PROJECT_SPEC.md`에서 링크가 잘 연결되는지 확인

### v0.0.19 — 지난 버전 확인 항목 (아직 확인 전)
- [ ] `AGENTS.md`를 열어서 "금지 규칙" 섹션이 잘 보이는지, 내용이 실제 상황과 맞는지 한 번 훑어봐주세요 (문서만 추가한 거라 화면엔 아무 변화 없음)

### v0.0.18 — 지난 버전 확인 항목 (아직 확인 전)
- [ ] 저장소 루트에 `AGENTS.md`/`ARCHITECTURE.md`가 있고, `docs/` 아래에 `PROJECT_SPEC.md`/`UI_GUIDELINE.md`/`TODO.md`/`CHANGELOG.md`가 있는지 확인
- [ ] 새 구조로 GitHub push 후 Vercel 배포가 평소처럼 정상 동작하는지 확인 (문서만 이동했고 앱 코드 경로는 안 바뀌었어야 함)
- [ ] (선택) 오프라인 상태에서 설정 탭의 다크모드/글자크기/가족공유 등이 정상 동작하는지 확인 — 서비스워커 캐시 목록 보강 확인용, 급하지 않으면 건너뛰어도 됨

### v0.0.17 — 지난 버전 확인 항목 (아직 확인 전)
- [ ] 캘린더에서 일정을 클릭했을 때 "병원명" 필드가 더 이상 안 보이는지 확인
- [ ] "내 일정"(직접 추가한 일정)을 수정 모달에서 열었을 때 제목·시작 시간·종료 시간을 바꿀 수 있고, 저장하면 캘린더에 그대로 반영되는지 확인
- [ ] 필수/추천/접종/정부지원 등 자동 일정은 기존처럼 제목 수정 없이 실제일·메모·완료만 수정되는지 확인
- [ ] 일정 추가 폼과 수정 모달의 "내 일정" 색상이 자유 색상 피커 대신 동그란 스와치 7개(하늘/민트/라벤더/로즈/피치/레몬/그레이) 중 고르는 방식으로 바뀌었는지 확인

### v0.0.16 — 지난 버전 확인 항목 (아직 확인 전)
- [ ] 캘린더 주간 뷰 헤더의 이유식 스티커 옆에 "이유식" 글자가 함께 보이는지 확인
- [ ] 설정 탭에 "캘린더 글자 크기" 항목이 보이고, 단계를 바꾸면 캘린더 안 일정 글자 크기만 바뀌는지(다른 글자는 영향 없는지) 확인
- [ ] 캘린더 화면 위쪽 "⚙️" 버튼을 누르면 설정 탭으로 바로 이동하는지 확인
- [ ] 홈 대시보드에서 "최근 접종" 카드가 없어졌는지, 체크리스트 카드가 "오늘" 카드 옆이 아니라 아래(둘째 줄)에 오는지 확인
- [ ] 홈 대시보드 체크리스트 카드의 "N/N 완료(전체)"가 "N/N 완료(필수)" 오른쪽에 한 줄로 붙어 보이는지 확인
- [ ] 홈 대시보드 카드들의 전체적인 높이가 이전보다 낮아졌는지(콤팩트해졌는지) 확인

### 지난 버전(20~30, v0.0.7~v0.0.15) — 옹짐꾼님 전체 확인 완료
과거 버전들의 확인 항목은 전부 점검 완료됨(옹짐꾼님 확인). 자세한 배포 이력은 CHANGELOG.md 참고.

### 참고 — 알아두면 좋은 것
- **`guide/` 페이지는 `data/checklist-data.js`나 `data/government-support.js`를 수정하면 자동으로 갱신되지 않음** — `node scripts/build-guide.mjs`를 다시 실행해야 반영됨
- **`data/government-support.js`의 `GOV_INFO_BASIS`는 제도 내용이 바뀔 때마다 함께 갱신해야 함**
- **`data/kr-holidays.js`는 매년 말 다음 해 공휴일이 확정되면 함께 갱신해야 함**
- **정적 파일(아이콘·CSS·JS) 내용을 바꾸면 `sw.js`의 `CACHE_NAME` 버전도 함께 올려야 기존 사용자에게 반영됨** (현재 v28) — 새 `js/` 파일을 추가할 땐 `APP_SHELL` 배열에도 추가할 것(v0.0.18에서 8개 누락분 발견·수정함)
- **버전을 올릴 땐 `index.html`의 `.site-footer-version`과 `scripts/build-guide.mjs`의 `APP_VERSION`도 반드시 함께 올리고 `node scripts/build-guide.mjs`를 재실행할 것** (v0.0.15~16에서 빼먹었던 항목 — 위 "🚨" 인계 노트 참고)
- **AdSense 실제 신청·심사는 진행하지 않음** — 정책 페이지만 준비해둔 상태

---

## 다음 후보 (미정 — 우선순위 논의 필요)

> v0.0.16부터 "고민 중" 항목도 이 섹션에서 함께 관리합니다.

- [ ] **홈 화면에 "오늘 해야 할 일" 요약 추가** — 오늘 날짜 기준 캘린더 일정·체크리스트 미완료 필수 항목을 한 곳에 모아 보여주는 카드. 지금은 "다음 일정" 카드가 있지만 "오늘 안에 뭘 해야 하는지"를 한눈에 모아 보여주진 않음
- [ ] **이모지 대신 실제 그림 파일(이미지 에셋) 추가** — 아이 프로필 아바타(아기 남아/여아 등, 옹짐꾼님이 예시로 주신 손그림 스타일 참고), 체크리스트 성장 단계(씨앗→나무→아이) 등을 유니코드 이모지 대신 실제 PNG/SVG 일러스트로 교체. Claude에게는 이미지 생성 도구가 없어서 실제 그림 파일을 옹짐꾼님이 만들어서 주셔야 진행 가능 — 받으면 `icons/` 폴더에 정식 에셋으로 추가
- [ ] **하드코딩된 파스텔 배경색 잔여분 그레이스케일 정리** (v0.0.20~21에서 대부분 정리했지만, 카드 hover 등 새로 발견되는 곳이 있으면 `docs/UI_GUIDELINE.md` 색상 예외 목록 기준으로 판단해서 정리)
- [ ] **체크리스트 항목 전면 추가/수정** — 옹짐꾼님이 세부 내용 주시면 진행
- [ ] FCM 진짜 푸시 알림 완성 (Firebase 콘솔 설정 필요)
- [ ] 성장 예측 고도화 — WHO 성장 곡선 기반으로 더 정교하게
- [ ] 전체적인 UI/비주얼 디자인 리뉴얼
- [ ] 다자녀 홈 대시보드 비교 뷰
- [ ] `data/who-growth.js`를 WHO 공식 LMS 파라미터로 교체
- [ ] 병원 방문 기록 상세 (진료과·의사 메모 등)
- [ ] AI 육아비서 구조 (OpenAI 연결 준비)
- [ ] 원더윅스(Wonder Weeks) 기간을 캘린더/체크리스트에 추가하는 것 고려
- [ ] 캘린더 앱인 만큼 첫 화면(홈)에 캘린더 자체가 보여야 하지 않을지 고민
- [ ] 임신 체크리스트 주차별 세분화 분류 (필수/추천/정부지원/병원/준비물/육아팁)
- [ ] 정부지원 항목별 정확한 신청 링크(딥링크)로 교체
- [ ] 제도 정보(금액·자격 요건) 연 1회 이상 최신화 필요
- [ ] **공휴일 이름을 날짜 옆에 표시할지** — 이유식 스티커랑 자리가 겹칠 수 있어서 아직 결정 안 함. 지금은 공휴일 날짜 숫자만 붉은색으로 표시하고 이름은 표시 안 함
- [ ] **스플래시 화면 추가** — 두 가지 방식이 있음:
  1. PWA 표준 스플래시(OS가 manifest.json 기반으로 자동 생성) — 이미 `manifest.json`의 `name`/`background_color`/`theme_color`/아이콘이 갖춰져 있어서 안드로이드는 추가 작업 없이 어느 정도 됨. iOS는 기기별 `apple-touch-startup-image` 이미지를 여러 장 등록해야 완벽해짐(번거로움)
  2. **앱 자체 스플래시**(추천) — `index.html`이 로드되자마자(Firebase/JS 로딩 전에) 로고+로딩 애니메이션이 담긴 전체화면 div를 먼저 보여주고, 앱 초기화(로그인 확인, 데이터 로드)가 끝나면 부드럽게 사라지게 하는 방식. OS나 브라우저에 따라 들쭉날쭉한 PWA 표준 스플래시보다 훨씬 일관되게 보이고, 지금 있는 다크모드 FOUC 방지 스크립트와 비슷한 패턴이라 구현 난이도도 낮음. 진행하게 되면 로고 파일(정사각형, 투명배경 PNG 권장)이 필요함

---

## 버그

> 발견된 버그는 여기에 추가하세요.

- (현재 없음)
