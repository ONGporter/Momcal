# TODO

> **Claude에게**: 이 파일에 있는 작업만 수행하세요.
> 완료한 항목은 이 파일에서 삭제하고 CHANGELOG.md에 기록하세요.
> PROJECT_SPEC.md와 UI_GUIDELINE.md를 먼저 읽고 시작하세요.
>
> **이 문서의 역할**: "지금 무엇을 해야 하는지"만 다룹니다 — 현재 스프린트 확인 항목, 다음 후보 기능, 버그.
> 프로젝트 구조·스키마·완료된 기능 이력 같은 참고 정보는 여기 적지 말고 **PROJECT_SPEC.md**에 적어주세요.
> 지난 스프린트의 확인 항목은 배포·확인이 끝나면 이 파일에서 지웁니다 (기록은 CHANGELOG.md에 이미 남아있음).

---

## ⚠️ 새 대화로 이어갈 때 먼저 읽어주세요 (Sprint 29 인계 노트)

옹짐꾼님이 Sprint 29 작업 후 새 대화로 이어가겠다고 하셔서 남겨두는 메모입니다.

### 미완성 — 다음에 이어서 해야 할 것
1. **FCM 진짜 푸시 알림 (요청했지만 1차 버전만 완료)**
   - 지금 있는 건 "앱을 열었을 때 확인하는 로컬 알림"(`js/notifications.js`)뿐, 앱을 꺼도 오는 진짜 푸시는 아직 없음
   - 완성하려면 **옹짐꾼님이 Firebase 콘솔에서 직접 해야 하는 일**이 있음 (Claude가 코드만으로 대신 못 함):
     1. Firebase 콘솔 → 프로젝트 설정 → Cloud Messaging 탭에서 사용 설정, **웹 푸시 인증서(VAPID 키)** 발급
     2. 요금제를 **Blaze(종량제)로 전환** — Cloud Functions를 쓰려면 무료 Spark 요금제로는 안 됨 (사용량이 적으면 사실상 무료에 가깝지만 신용카드 등록은 필요)
     3. 위 두 가지를 완료하고 VAPID 키를 Claude에게 전달해주면, 그다음부터 `firebase-messaging-sw.js` 추가 + 알림 구독 코드 + "예방접종 하루 전/정부지원 마감 3일 전"처럼 실제로 알림을 보내는 Cloud Functions 스케줄러까지 이어서 구현 가능
   - `sw.js`에 `push`/`notificationclick` 핸들러는 미리 만들어둬서, 위 설정만 끝나면 바로 이어붙일 수 있음
2. **폰트 전체 스윕 미완료**: Paperlogy(제목)/Pretendard(본문·버튼)를 공용 클래스(`.btn`, `h1~h3`, `.sec` 등) 위주로만 적용함. 앱 구석구석 수백 곳의 개별 `font-weight` 인라인 스타일까지는 다 못 바꿈 — 특정 화면에서 폰트가 안 바뀐 곳이 보이면 캡처해서 알려주면 바로 고칠 수 있음
3. **성장 예측은 아주 단순한 선형 추정**(최근 2개 기록의 증가율을 그대로 연장)임 — 더 정교하게 하려면(예: WHO 성장 곡선 기반 예측, 나이에 따라 증가 속도가 느려지는 것 반영) 추가 작업 필요

### 이번에 고친 중요 버그 (재확인 꼭 필요)
- **캘린더 타임존 버그**: 한국시간 자정~오전 9시 사이에 캘린더가 하루 전 날짜로 보이던 문제 수정함 (`today()` 함수가 UTC 대신 KST 기준으로 계산하도록 변경). **다음 새벽에 실제로 확인해보고 알려주세요** — 이게 제일 중요한 확인 사항
- 아기 생후 일수를 "태어난 날 = 1일째"로 세도록 변경함 (기존엔 0일째였음)

### 계속 지켜야 할 원칙 (매번 리마인드됨)
- 기존 기능 절대 삭제 금지, 새 프로젝트 생성 금지, Firebase 구조 유지, 기존 UI 최대한 유지, 모든 기능 모듈화, 복잡한 기능보다 사용성 우선, "부모가 매일 여는 앱" 목표

---

## 현재 확인 필요 항목

### Sprint 29 (이번 스프린트, 분량이 많아 꼭 하나씩 확인 부탁드려요)
- [ ] **캘린더 타임존 버그** — 새벽 시간대(특히 자정~오전 9시)에 캘린더 날짜가 정확한지 확인 (가장 중요)
- [ ] 아기 프로필의 "오늘 며칠째"가 태어난 날 기준 1일째로 보이는지 확인
- [ ] 전체 폰트가 제목은 Paperlogy, 본문은 Pretendard로 바뀌어 보이는지 확인
- [ ] topbar 로고 마스코트 이미지가 더 커진 게 자연스러운지 확인
- [ ] 육아정보 페이지 — 카테고리 탭 드래그/휠 스크롤, 페이지 이동 탭(상단), 맨 위로 가기 버튼이 잘 동작하는지 확인
- [ ] 체크리스트 사이드바 하단 "육아정보 검색" 박스에서 검색 시 새 탭으로 육아정보 페이지가 열리고 자동 검색되는지 확인
- [ ] 정부지원 탭/페이지에 "2026년 7월 기준" 문구가 잘 보이는지 확인
- [ ] 홈 화면 이모지(🌱💕) 제거로 배너가 짧아졌는지, 화면 최하단에 "v0.0.1" 버전이 보이는지 확인
- [ ] 로고를 탭하면 홈으로 이동하는지 확인
- [ ] 성장그래프에 "🔮 한 달 뒤 예상" 카드가 기록 2개 이상일 때 뜨는지 확인
- [ ] 홈 화면 "🔔 알림 받기" 버튼 → 권한 허용 후 오늘 일정이 있을 때 알림이 뜨는지 확인 (앱이 열려있을 때만 동작하는 1차 버전임)

### 지난 스프린트(20~28) — 요약
Sprint 14~19는 실사용 확인 완료. Sprint 20~28은 대부분 미확인 상태로 누적되어 있었는데, 이번에 정리하면서 핵심만 남겼습니다:

- [ ] Google Search Console — sitemap 상태 확인, "맘캘" 검색 노출 여부 계속 지켜보기
- [ ] 정부지원 마감임박 강조(빨간 테두리)가 `deadlineDate` 있는 4개 항목(출생신고·부모급여·아동수당·양육수당)에서만 뜨는 게 맞는지 (나머지 12개는 정확한 날짜가 없어 대상 제외됨)
- [ ] 캘린더 색상 커스터마이징(하단 범례 탭해서 색 변경)이 새로고침 후에도 유지되는지
- [ ] 아이콘 화질 — 원본이 목업이라 화질이 완벽하진 않음. 더 선명하게 하려면 512px 이상 정사각형 단일 파일로 다시 전달 필요
- [ ] 게스트 데이터 자동 이전은 "완전히 새 계정"일 때만 동작 (기존 계정 로그인 시 수동 병합 없음 — 알고 있으면 되는 사항)

### 참고 — 알아두면 좋은 것
- **`guide/` 페이지는 `data/checklist-data.js`나 `data/government-support.js`를 수정하면 자동으로 갱신되지 않음** — `node scripts/build-guide.mjs`를 다시 실행해야 반영됨
- **`data/government-support.js`의 `GOV_INFO_BASIS`("2026년 7월 기준")는 제도 내용이 바뀔 때마다 함께 갱신해야 함**
- **정적 파일(아이콘·CSS·JS) 내용을 바꾸면 `sw.js`의 `CACHE_NAME` 버전도 함께 올려야 기존 사용자에게 반영됨** (현재 v7)
- **AdSense 실제 신청·심사는 진행하지 않음** — 정책 페이지만 준비해둔 상태

---

## 다음 후보 (미정 — 우선순위 논의 필요)

- [ ] FCM 진짜 푸시 알림 완성 — 위 "인계 노트" 참고 (Firebase 콘솔 설정 필요)
- [ ] 성장 예측 고도화 — WHO 성장 곡선 기반으로 더 정교하게(현재는 단순 선형 추정)
- [ ] 전체적인 UI/비주얼 디자인 리뉴얼
- [ ] 가족 공유 구조 — 각자 로그인해도 실시간 공동 편집되는 진짜 멀티 계정 공유 (현재는 링크 공유 수준)
- [ ] 다크모드
- [ ] 예방접종 기록 PDF/이미지 내보내기
- [ ] 다자녀 홈 대시보드 비교 뷰
- [ ] `data/who-growth.js`를 WHO 공식 LMS 파라미터로 교체 (현재는 근사 med/sd 참고값)
- [ ] 병원 방문 기록 상세 (진료과·의사 메모 등)
- [ ] AI 육아비서 구조 (OpenAI 연결 준비)
- [ ] 임신 체크리스트 주차별 세분화 분류 (필수/추천/정부지원/병원/준비물/육아팁)
- [ ] 정부지원 항목별 정확한 신청 링크(딥링크)로 교체
- [ ] 제도 정보(금액·자격 요건) 연 1회 이상 최신화 필요

---

## 버그

> 발견된 버그는 여기에 추가하세요.

- (현재 없음 — Sprint 29에서 타임존 버그 수정 완료)
