# TODO

> **Claude에게**: 이 파일에 있는 작업만 수행하세요.
> 완료한 항목은 이 파일에서 삭제하고 CHANGELOG.md에 기록하세요.
> PROJECT_SPEC.md와 UI_GUIDELINE.md를 먼저 읽고 시작하세요.
>
> **이 문서의 역할**: "지금 무엇을 해야 하는지"만 다룹니다 — 현재 버전 확인 항목, 다음 후보 기능, 버그.
> 프로젝트 구조·스키마·완료된 기능 이력 같은 참고 정보는 여기 적지 말고 **PROJECT_SPEC.md**에 적어주세요.
> 지난 버전의 확인 항목은 배포·확인이 끝나면 이 파일에서 지웁니다 (기록은 CHANGELOG.md에 이미 남아있음).
> **v0.0.2부터**: "스프린트"라는 용어 대신 "버전"(v0.0.x)으로 통일해서 부릅니다 (Sprint 1~28은 버전 체계 도입 이전 기록이라 그대로 둠, Sprint 29 = v0.0.1).

---

## ⚠️ 새 대화로 이어갈 때 먼저 읽어주세요 (v0.0.9 인계 노트)

지금까지의 자세한 변경 이력은 CHANGELOG.md에 버전별로 다 남아있고, PROJECT_SPEC.md·UI_GUIDELINE.md는 항상 최신 상태로 맞춰뒀으니 이 파일들을 먼저 읽으면 맥락 파악이 될 거예요.

### 미완성 — 다음에 이어서 해야 할 것
1. **FCM 진짜 푸시 알림** — 아직 "앱을 열었을 때 확인하는" 방식(`js/notifications.js`)뿐, 앱을 꺼도 오는 진짜 푸시는 아님. 완성하려면 옹짐꾼님이 Firebase 콘솔에서 직접 해야 하는 일(Cloud Messaging 사용 설정+VAPID 키 발급, Blaze 요금제 전환)이 있음 — 준비되면 알려주시면 이어서 구현 가능. `sw.js`에 `push`/`notificationclick` 핸들러는 미리 만들어둠
2. **성장 예측은 아주 단순한 선형 추정**(최근 2개 기록의 증가율을 그대로 연장)임 — WHO 성장 곡선 기반 예측처럼 정교하게 하려면 추가 작업 필요
3. **다크 모드가 앱 전체 하드코딩 색상을 100% 커버하진 못했을 수 있음** — v0.0.8, v0.0.9에서 캡처 주신 부분과 추가로 발견한 부분들을 계속 보정 중이지만, 매번 새로 발견되는 걸 보면 아직 못 찾은 구석이 남아있을 가능성이 높음. 발견되면 캡처로 알려주면 바로 보정 가능
4. **momcal.app 도메인 연결 후속 조치 필요** (아래 "도메인 연결 후속 조치" 항목 참고) — Vercel 연결은 완료됐지만 Firebase Auth·Search Console·Naver 서치어드바이저 쪽은 콘솔에서 옹짐꾼님이 직접 추가 설정해야 함
5. **글자 크기 조절 5단계 전환 직후라 실제 기기에서 최종 확인 필요** — 특히 "매우 크게"(23px)에서 레이아웃이 깨지는 화면이 있는지, 홈 대시보드 새 레이아웃(모바일 2열+1열 혼합)이 여러 화면 크기에서 다 괜찮은지 확인 권장

### 도메인 연결 후속 조치 (momcal.app, v0.0.9)
Vercel에 커스텀 도메인 연결은 완료됐고, 코드 안에서 도메인을 참조하던 곳(canonical/OG 태그, sitemap.xml, robots.txt, 가족 공유 링크 등)은 전부 `momcal.app`으로 바꿔뒀습니다. 다만 아래는 각 서비스 콘솔에서 옹짐꾼님이 직접 해줘야 하는 부분이라 코드로는 처리 못 했어요:
- [ ] **Firebase Auth → 승인된 도메인(Authorized domains)에 `momcal.app` 추가** — 안 하면 새 도메인에서 로그인(이메일/구글) 자체가 막힐 수 있음 (Firebase 콘솔 → Authentication → Settings)
- [ ] **Google OAuth 클라이언트의 승인된 자바스크립트 원본에도 `https://momcal.app` 추가** — 구글 로그인 버튼 관련
- [ ] **Google Search Console에 `momcal.app` 속성 새로 등록 + 새 sitemap.xml 제출** — 기존 `momcal.vercel.app` 속성과는 별개로 잡힘. 최종적으로 어느 도메인을 정식 주소로 쓸지 정해지면 기존 vercel.app 속성에서 새 도메인으로 이전 설정(301 등)도 고려
- [ ] **네이버 서치어드바이저도 동일하게 `momcal.app` 재등록**
- [ ] `momcal.vercel.app`으로 들어오는 기존 방문자·북마크가 있을 수 있으니, 당분간 두 도메인 다 접속되게 유지할지(Vercel은 기본적으로 둘 다 열어둠) 결정 필요

### 알아두면 좋은 것
- 정식 도메인은 `momcal.app`(Vercel 커스텀 도메인 연결 완료, v0.0.9) — `momcal.vercel.app`도 계속 접속은 가능
- 폰트 2종 체제: 제목/본문/버튼/로고는 "온글잎 박다현체", 체크리스트 세부 설명·육아정보 설명·안내 문구 등 "정보 글"은 "오뮤 다예쁨체" — 둘 다 Bold 파일이 없어 `font-weight: normal` 고정돼있음
- 하단 탭: 홈/캘린더/체크리스트/성장/설정 5개, "설정" 탭만 폭이 좁게 디자인됨(`css/main.css`의 `.nav-pills`)
- 설정 탭엔 다크 모드, 글자 크기(5단계), 배우자와 함께 쓰기, 알림(카테고리별 on/off+시간대)이 있고, "어플로 추가"만 홈 화면에 별도로 남아있음
- 육아정보 페이지(`guide/`)는 다크 모드·글자 크기 설정을 앱과 동일하게 따라가지만, 그 페이지 자체에 설정 토글은 없음(앱에서 고른 값을 읽기만 함)
- 체크리스트 실시간 동기화(onSnapshot) 관련 코드를 만질 땐 `js/state.js`의 `subscribeToUserData` 주석 참고 — 내 기기가 방금 저장한 내용의 "로컬 에코"와 실제 원격 변경을 `hasPendingWrites`로 구분해야 화면이 불필요하게 중복 렌더링되지 않음(v0.0.9에서 배지 이중 깜빡임 버그로 한 번 걸렸던 부분)

### 계속 지켜야 할 원칙 (매번 리마인드됨)
- 기존 기능 절대 삭제 금지, 새 프로젝트 생성 금지, Firebase 구조 유지, 기존 UI 최대한 유지, 모든 기능 모듈화, 복잡한 기능보다 사용성 우선, "부모가 매일 여는 앱" 목표

---

## 현재 확인 필요 항목

### v0.0.9 (이번 버전 — 다크모드 추가 보정, 체크리스트 대시보드 카드 개선, 배지 이중 깜빡임 버그 수정, momcal.app 도메인 연결)
- [ ] 다크 모드에서 가족 프로필 카드(선택됨/등록하기), 체크리스트 탭 버튼, "현재 N개월" 배너, 체크리스트 배지(Perfect/Master/Legend), 정부지원 상태 배지, 성장그래프 안내문구가 이제 흰/밝은 배경 없이 잘 보이는지 확인
- [ ] 홈 대시보드 "체크리스트" 카드에 다른 카드처럼 강조색 문구가 뜨는지 — 배지 획득 전엔 다음 추천 항목, 획득 후엔 배지 현황이 보이는지 확인
- [ ] 체크리스트 항목 체크해서 배지(Perfect/Master/Legend) 얻을 때 애니메이션이 1번만 재생되는지 확인 (기존엔 저장 완료 시점에 한 번 더 깜빡였음)
- [ ] momcal.app 도메인으로 접속했을 때 정상 동작하는지, 로그인(이메일/구글)이 막히지 않는지 확인 — 막히면 위 "도메인 연결 후속 조치"의 Firebase Auth 승인된 도메인 설정을 먼저 확인

### v0.0.8 — 지난 버전 확인 항목
- [ ] 다크 모드에서 캡처 주셨던 화면들(홈 팁 카드, 체크리스트, 성장 기록 목록, 로그인 화면)이 흰 배경 없이 잘 보이는지 확인
- [ ] 육아정보 페이지·개인정보처리방침 등에서도 앱과 같은 다크 모드가 적용되는지 확인
- [ ] 설정 탭에서 글자 크기가 "매우 작게/작게/보통/크게/매우 크게" 5단으로 나오고, 기본값이 예전 "크게"(19px) 위치인지 확인
- [ ] 홈 대시보드에서 "은유 오늘"과 "체크리스트"가 웹·모바일 모두 첫 줄에 나란히 보이고, 나머지 카드는 모바일에서 한 줄씩 차지하는지 확인

### v0.0.7 — 지난 버전 확인 항목
- [ ] 하단 탭에서 "설정" 탭만 좁고, 홈/캘린더/체크리스트/성장은 예전처럼 넓어 보이는지 확인
- [ ] 알림 설정에서 "오늘 일정 알림"/"정부지원 마감 알림"을 각각 꺼봤을 때 그 종류만 안 뜨는지 확인
- [ ] 알림 시간대를 "오전에만" 등으로 바꿔보고, 그 시간대가 아닐 때 앱을 열면 알림이 안 뜨는지 확인

### 지난 스프린트(20~28) — 요약
Sprint 14~19는 실사용 확인 완료. Sprint 20~28은 대부분 미확인 상태로 누적되어 있었는데, 이번에 정리하면서 핵심만 남겼습니다:

- [ ] Google Search Console — sitemap 상태 확인, "맘캘" 검색 노출 여부 계속 지켜보기
- [ ] 정부지원 마감임박 강조(빨간 테두리)가 `deadlineDate` 있는 4개 항목(출생신고·부모급여·아동수당·양육수당)에서만 뜨는 게 맞는지 (나머지 12개는 정확한 날짜가 없어 대상 제외됨)
- [ ] 캘린더 색상 커스터마이징(하단 범례 탭해서 색 변경)이 새로고침 후에도 유지되는지
- [ ] 아이콘 화질 — 원본이 목업이라 화질이 완벽하진 않음. 더 선명하게 하려면 512px 이상 정사각형 단일 파일로 다시 전달 필요
- [ ] 게스트 데이터 자동 이전은 "완전히 새 계정"일 때만 동작 (기존 계정 로그인 시 수동 병합 없음 — 알고 있으면 되는 사항)

### 참고 — 알아두면 좋은 것
- **`guide/` 페이지는 `data/checklist-data.js`나 `data/government-support.js`를 수정하면 자동으로 갱신되지 않음** — `node scripts/build-guide.mjs`를 다시 실행해야 반영됨
- **`data/government-support.js`의 `GOV_INFO_BASIS`("2026년 7월 기준")는 제도 내용이 바뀔 때마다 함께 갱신해야 함**
- **정적 파일(아이콘·CSS·JS) 내용을 바꾸면 `sw.js`의 `CACHE_NAME` 버전도 함께 올려야 기존 사용자에게 반영됨** (현재 v7)
- **AdSense 실제 신청·심사는 진행하지 않음** — 정책 페이지만 준비해둔 상태

---

## 다음 후보 (미정 — 우선순위 논의 필요)

- [ ] FCM 진짜 푸시 알림 완성 — 위 "인계 노트" 참고 (Firebase 콘솔 설정 필요)
- [ ] 성장 예측 고도화 — WHO 성장 곡선 기반으로 더 정교하게(현재는 단순 선형 추정)
- [ ] 전체적인 UI/비주얼 디자인 리뉴얼
- [ ] 가족 공유 구조 — 각자 로그인해도 실시간 공동 편집되는 진짜 멀티 계정 공유 (현재는 링크 공유 수준)
- [ ] 예방접종 기록 PDF/이미지 내보내기
- [ ] 다자녀 홈 대시보드 비교 뷰
- [ ] `data/who-growth.js`를 WHO 공식 LMS 파라미터로 교체 (현재는 근사 med/sd 참고값)
- [ ] 병원 방문 기록 상세 (진료과·의사 메모 등)
- [ ] AI 육아비서 구조 (OpenAI 연결 준비)
- [ ] 원더윅스(Wonder Weeks) 기간을 캘린더/체크리스트에 추가하는 것 고려 (v0.0.2에서 요청)
- [ ] 스티커 위치를 이유식 관련 날짜 숫자 옆으로 옮기는 것 고려 (v0.0.2에서 요청)
- [ ] 캘린더 앱인 만큼 첫 화면(홈)에 캘린더 자체가 보여야 하지 않을지 고민 (v0.0.2에서 요청)
- [ ] 임신 체크리스트 주차별 세분화 분류 (필수/추천/정부지원/병원/준비물/육아팁)
- [ ] 정부지원 항목별 정확한 신청 링크(딥링크)로 교체
- [ ] 제도 정보(금액·자격 요건) 연 1회 이상 최신화 필요

---

## 버그

> 발견된 버그는 여기에 추가하세요.

- (현재 없음 — Sprint 29에서 타임존 버그 수정 완료)
